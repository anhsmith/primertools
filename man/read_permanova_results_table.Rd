% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permanova.R
\name{read_permanova_results_table}
\alias{read_permanova_results_table}
\title{Read PRIMER PERMANOVA "table of results" from a text output file}
\usage{
read_permanova_results_table(path, encoding = "UTF-8")
}
\arguments{
\item{path}{Path to a PRIMER PERMANOVA text output file.}

\item{encoding}{File encoding passed to \code{\link[=readLines]{readLines()}}. Default \code{"UTF-8"}.}
}
\value{
A data frame containing the PERMANOVA results table.
}
\description{
Parses the \strong{PERMANOVA table of results} section from a PRIMER PERMANOVA
text output file into a data frame.
}
\details{
PRIMER prints the label for the permutations column across two lines
(i.e., \code{Unique} on one line and \code{perms} on the next) but they can
sometimes be misaligned in plain-text exports.
This function ignores the line with just \code{Unique} and simply
renames the \code{perms} column to \verb{Unique perms} later for clarity.
}
\examples{
f <- system.file("extdata", "PERMANOVA1.txt", package = "primertools")
if (file.exists(f)) {
  tab  <- read_permanova_results_table(f)
  covs <- read_permanova_components_of_variation(f)
}
}
