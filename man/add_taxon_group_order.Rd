% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simper_helpers.R
\name{add_taxon_group_order}
\alias{add_taxon_group_order}
\title{Add a within-facet ordering variable for SIMPER plots}
\usage{
add_taxon_group_order(
  d_within,
  group_var = "group",
  taxon_var = "taxon",
  order_var = "contrib",
  decreasing = TRUE
)
}
\arguments{
\item{d_within}{A tibble from \code{tidy_simper_within()}.}

\item{group_var}{Column name giving the facet/group (default \code{group}).}

\item{taxon_var}{Column name giving the taxon label (default \code{taxon}).}

\item{order_var}{Column used to order taxa within group (default \code{contrib}).}

\item{decreasing}{If TRUE (default), order from largest to smallest.}
}
\value{
The same tibble with an additional factor column \code{taxon_group}.
}
\description{
ggplot2 orders factors globally, so faceted SIMPER plots need a plotting-only
factor that is ordered within each group. This function adds \code{taxon_group}.
}
\examples{
# d_within <- tidy_simper_within(simper_site_cp)
# d_within <- add_taxon_group_order(d_within)
}
