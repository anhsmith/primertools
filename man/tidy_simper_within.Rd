% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simper_helpers.R
\name{tidy_simper_within}
\alias{tidy_simper_within}
\title{Tidy a SIMPER object (within-group tables)}
\usage{
tidy_simper_within(simper_obj, cum_max = 70)
}
\arguments{
\item{simper_obj}{A SIMPER object produced by \code{primertools} (must contain \verb{$within}).}

\item{cum_max}{Cumulative cutoff (percent). Keep rows with \code{cum <= cum_max}.}
}
\value{
A tibble with one row per taxon per group.
}
\description{
Convert a \code{primertools} SIMPER object into a single tibble for within-group
results, optionally truncating by cumulative contribution.
}
\examples{
# d_within <- tidy_simper_within(simper_site_cp, cum_max = 70)
}
