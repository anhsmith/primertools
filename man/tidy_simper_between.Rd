% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simper_helpers.R
\name{tidy_simper_between}
\alias{tidy_simper_between}
\title{Tidy a SIMPER object (between-group tables)}
\usage{
tidy_simper_between(simper_obj, cum_max = 70)
}
\arguments{
\item{simper_obj}{A SIMPER object produced by \code{primertools} (must contain \verb{$between}).}

\item{cum_max}{Cumulative cutoff (percent). Keep rows with \code{cum <= cum_max}.}
}
\value{
A tibble with one row per taxon per contrast.
}
\description{
Convert a \code{primertools} SIMPER object into a single tibble for between-group
results, optionally truncating by cumulative contribution.
}
\examples{
# d_between <- tidy_simper_between(simper_site_cp, cum_max = 60)
}
