# primertools 0.1.1
- Add `aic_permanova()` and `get_permanova_aic()` for AIC-like comparison of PRIMER PERMANOVA models.
- Add vignette: “Reading PRIMER PERMANOVA outputs”.

## primertools 0.1.0
- Added read_permanova_*() helpers to parse PRIMER PERMANOVA output.
